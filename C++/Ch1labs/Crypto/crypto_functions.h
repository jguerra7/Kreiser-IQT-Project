#include <iostream>
#include <string>

//This function will encode the string that the user inputs
int encode_string(std::string message, int key);

//This function will decode the string after it has been encoded
int decode_string(std::string message, int key);
